name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Render Service
        run: |
          curl -s https://ai-legal-assistance-co-pilot.onrender.com/keep-alive || echo "Failed"
